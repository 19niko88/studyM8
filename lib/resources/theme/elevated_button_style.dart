import 'package:flutter/material.dart';

ButtonStyle onboardingElevatedButtonStyle = ElevatedButton.styleFrom(
    backgroundColor: Color(0XffFFFFFF),
    shape: RoundedRectangleBorder(
        borderRadius:
        BorderRadius.all(Radius.circular(10.0))));

ButtonStyle getYouInElevatedButtonStyle = ElevatedButton.styleFrom(
    backgroundColor: Color(0Xff3D348B),
    shape: RoundedRectangleBorder(
        borderRadius:
        BorderRadius.all(Radius.circular(15.0))));

ButtonStyle signUpElevatedButtonStyle = ElevatedButton.styleFrom(
    backgroundColor: Color(0Xff3D348B),
    shape: RoundedRectangleBorder(
        borderRadius:
        BorderRadius.all(Radius.circular(10.0))));

ButtonStyle logInElevatedButtonStyle = ElevatedButton.styleFrom(
    backgroundColor: Color(0XffFFFFFF),
    shape: RoundedRectangleBorder(
        borderRadius:
        BorderRadius.all(Radius.circular(10.0))));

ButtonStyle signUpElevatedButtonStyle1 = ElevatedButton.styleFrom(
    backgroundColor: Color(0Xff3D348B),
    shape: RoundedRectangleBorder(
        borderRadius:
        BorderRadius.all(Radius.circular(8.0))));